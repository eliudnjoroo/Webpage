name: Jekyll
build:
  base: /opt/build/repo/Home
  command: |
    gem install bundler
    bundle install
    bundle exec jekyll build
  commandOrigin: ui
  environment:
    - jekyll
  publish: /opt/build/repo/Home
  publishOrigin: ui
